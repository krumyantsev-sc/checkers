import React from 'react';
import "../styles/SideMenu.css";

const SideMenu = () => {
    return (
        <div className="sidenav">
            <a href="#">Home</a>
            <a href="#">Games</a>
            <a href="#">Profile</a>
            <a href="#">Contact</a>
        </div>
    );
};

export default SideMenu;