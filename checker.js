export default class checker {
    isLady = false;
    constructor(color, divId, position,div) {
        this.color = color;
        this.divId = divId;
        this.position = position;
        this.div = div;
    }

    color;
    position;
    divId;
    div;
}